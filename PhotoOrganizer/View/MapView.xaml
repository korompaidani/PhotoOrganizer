<UserControl x:Class="PhotoOrganizer.UI.View.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoOrganizer.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Label Content="Location Name"/>
            <TextBox Text="{Binding Location.LocationName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Set Coordinate" Click="OnSetCoordinateButtonClick"/>
            <Button Content="Save Coordinate" Click="OnSaveButtonClick"/>
            <Button Content="X" Background="Transparent" Command="{Binding CloseMapCommand}"/>
            <Button Content="Alter Save" Background="Transparent" Command="{Binding SaveCommand}"/>
        </StackPanel>
        
        
        <WebBrowser Grid.Row="1" x:Name="mapBrowser" Source="https://www.google.com/maps" LoadCompleted="OnLoadCompleted"/>
        
    </Grid>
</UserControl>
